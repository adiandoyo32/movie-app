import Button from '../components/button/Button'
import HeroSlide from '../components/hero-slide/HeroSlide'

const Home = () => {
  return (
    <div>
      <HeroSlide />
      <Button>
        Save
      </Button>
    </div>
  )
}

export default Home